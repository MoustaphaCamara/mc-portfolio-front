# Dockerfile.dev
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --silent

COPY . .

# Vite dev server default: 5198
EXPOSE 5198

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5198"]
